# pathlib_gui

`pathlib_gui` is a Tkinter-based filesystem workbench for local paths and archive files. Start with the [overview](overview/README.md), install it from [PyPI or source](installation.md), then use the [quick start](usage/quickstart.md) for the main launch patterns and workflows.
